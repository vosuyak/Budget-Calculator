import { FirstCapatializePipe } from './first-capatialize.pipe';

describe('FirstCapatializePipe', () => {
  it('create an instance', () => {
    const pipe = new FirstCapatializePipe();
    expect(pipe).toBeTruthy();
  });
});
